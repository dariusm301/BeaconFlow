import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.beaconflow.app',
  appName: 'beaconflow',
  webDir: 'dist/beaconflow/browser',

};


export default config;
